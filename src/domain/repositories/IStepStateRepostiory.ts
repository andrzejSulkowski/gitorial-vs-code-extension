/*
Defines how the state of individual steps (e.g., completion, current status) is persisted and retrieved (e.g., getStepState(tutorialId: TutorialId, stepId: string): Promise<StepState | null>, saveStepState(tutorialId: TutorialId, stepId: string, state: StepState): Promise<void>)
*/